// homography.js — Stub for perspective transform (Wave 2)
// Will be used by non-marker instruments that need perspective correction

/**
 * Compute a 3x3 homography matrix from source points to destination points.
 * Stub — not implemented until Wave 2.
 *
 * @param {Array} _srcPoints - Source point pairs
 * @param {Array} _dstPoints - Destination point pairs
 * @throws {Error} Always — not yet implemented
 */
export function computeHomography(/* srcPoints, dstPoints */) {
  throw new Error('homography: not implemented until Wave 2')
}

/**
 * Transform a point using a homography matrix.
 * Stub — not implemented until Wave 2.
 *
 * @param {Array} _H - 3x3 homography matrix
 * @param {number} _x - X coordinate
 * @param {number} _y - Y coordinate
 * @throws {Error} Always — not yet implemented
 */
export function transformPoint(/* H, x, y */) {
  throw new Error('homography: not implemented until Wave 2')
}
